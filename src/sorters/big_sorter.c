#include <push_swap.h>

void	big_sorter(t_stack *stack_a, t_stack *stack_b)
{
	// int	pivot;
	// int	i;
	if (stack_a && stack_b)
		ft_putendl_fd("Doing big sorter (change the name of function)", 1);
}
